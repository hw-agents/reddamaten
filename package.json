{
  "name": "reddamaten",
  "version": "1.0.0",
  "description": "ReddaMaten - Food Waste Reduction Marketplace",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon",
    "build": "cd client && npm run build",
    "postinstall": "[ -d client ] && cd client && npm install || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "redis": "^4.6.12",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
